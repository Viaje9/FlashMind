## 1. 共享音軌核心改造

- [x] 1.1 重構 `SpeakingAudioPlayerService`，改為單一長壽命 `HTMLAudioElement` 播放架構。
- [x] 1.2 新增共享音軌控制介面（啟用、停用、靜音）與 keep-alive 機制，並限制只在共享模式啟用。
- [x] 1.3 保留並驗證既有播放控制（播放/暫停/重試/錯誤）在共享音軌模式下仍可運作。

## 2. 口說頁播放流程整合

- [x] 2.1 在 `/speaking` 錄音入口整合共享音軌啟用，確保按下錄音即完成播放通道初始化。
- [x] 2.2 依錄音與送出狀態套用 `mute` 控制，實作「錄音/等待回應時靜音、不暫停」策略。
- [x] 2.3 在口說頁離開或銷毀時釋放共享音軌，避免影響其他頁面播放路徑。

## 3. AI 助手片段發音路徑整合

- [x] 3.1 調整 `TtsStore` 讓 `/speaking` 的片段發音改走共享播放器，保留原有 loading/playing/error 行為。
- [x] 3.2 確認片段發音快取與播放鍵值管理可與共享音軌共存，不造成狀態錯置。
- [x] 3.3 處理片段發音錯誤訊息隔離，避免污染口說全域錯誤呈現。

## 4. 測試與驗證

- [x] 4.1 擴充播放器單元測試，覆蓋共享音軌重用與靜音切換行為。
- [x] 4.2 更新口說頁/相關 store 測試 mock 與案例，覆蓋共享音軌控制介面。
- [x] 4.3 執行 `apps/web` 測試與 build，確認無編譯錯誤與主要播放流程回歸。
